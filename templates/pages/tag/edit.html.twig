{% extends 'base.html.twig' %}

{% block title %}Edit Tag{% endblock %}

{% block body %}
    <h1>Edit Tag</h1>

    {{ include('pages/tag/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_tag_index') }}">{{ ux_icon('bi:arrow-left') }}back to list</a>

    {{ include('pages/tag/_delete_form.html.twig') }}
{% endblock %}
